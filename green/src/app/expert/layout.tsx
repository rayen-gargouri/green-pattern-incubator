import { ExpertLayout } from "@/components/layouts/ExpertLayout";

export default function ExpertRouteLayout({ children }: { children: React.ReactNode }) {
  return <ExpertLayout>{children}</ExpertLayout>;
}
